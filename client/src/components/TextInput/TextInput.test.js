import React from "react";
import toJson from "jest-enzyme";

// component
import TextInput from "./TextInput";

// utils
import { setShallow, findByTestAttr, checkProps } from "../../tests/testUtils";

describe("<TextInput />", () => {
  it("renders without an error", () => {});
});
